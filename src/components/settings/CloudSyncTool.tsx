import { AccountBackupPanel } from '@/components/settings/AccountBackupPanel'

/** @deprecated Use AccountBackupPanel in Settings → 個人 → 帳號與備份 */
export function CloudSyncTool() {
  return <AccountBackupPanel />
}
